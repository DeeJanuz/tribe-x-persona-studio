# Documentation Updates

All documentation updates must be handled by the `doc-maintainer` agent.

- Do NOT manually edit documentation files (docs/, CLAUDE.md)
- After commits or feature implementations, launch the `doc-maintainer` agent
- The agent will update appropriate files in `docs/` and archive TDD artifacts
