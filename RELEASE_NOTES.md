# Unreleased

- Persona Studio now includes a Test Suites authoring tab for synthetic consultant scenarios, deterministic/manual checks, background suite runs, and customer-safe reports.
- Persona Studio now exposes MCP tools for consultant persona create, update, archive, restore, listing, and synthetic test-suite creation/runs through the dev control-plane API instead of direct database/Wrangler edits.
- Persona Studio now has a Vitest + happy-dom renderer test suite covering Test Suites launch, review/report states, and legacy plugin naming suppression.
- Persona Studio now preserves multiline formatting when authors insert platform default rules.
- Persona Studio now has a collapsible persona sidebar and shows the active persona name in the editor top bar.
- Persona Studio now lets authors collapse and reopen the Default Rule Library inside the rules editor.
- Persona Studio now uses a persistent wizard stepper for persona authoring, with tabs for Overview, Instructions, Runtime, Skills, and Review & Test.
- Persona Studio now shows platform default rules in a Default Rule Library inside the rules editor, with one-click insertion into persona drafts.
- Persona Studio now defaults to the alphabetically first consultant organization and uses a compressed one-line workspace header without repeating the selected organization notice.
