# Unreleased

- Fix consultant organization selection when the MCPViews host client omits `organization.kind` by loading raw `/organizations` first and preserving known consultant org kind during native state sync.
